import Stack from '@mui/material/Stack';
import AppBar from '@mui/material/AppBar';

type Props = {}

const SideBar = (props: Props) => {
  return (

        <div className='w-[250px] bg-blue-500'>SideBar</div>

  )
}

export default SideBar